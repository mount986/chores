services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data   # persists the SQLite database file
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-before-production}
      - DATABASE_URL=sqlite:////app/data/chores.db
    restart: unless-stopped
