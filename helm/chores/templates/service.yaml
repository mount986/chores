apiVersion: v1
kind: Service
metadata:
  name: {{ include "chores.name" . }}
  labels:
    {{- include "chores.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "chores.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
